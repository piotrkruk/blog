<p>
	<strong>Title:</strong>
	<%= @article.title %>
</p>
<p>
	<strong>Text:</strong>
	<%= @article.text %>
</p>
<p>
	<i>
	Author: 
	<%= link_to @article.user.email, @article.user %>
	</i>
</p>

<%= render 'comments/comments', comments: @article.comments.includes(:user) %>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>